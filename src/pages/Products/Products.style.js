import fonts from "../../contants/fonts";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    fontSize: fonts.fontSize,
    color: fonts.color,
    fontWeigth: fonts.fontWeigth,
  },
});

export default styles;
